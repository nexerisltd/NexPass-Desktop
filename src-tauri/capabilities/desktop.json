{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-capability",
  "description": "Desktop-only permissions for NexPass's main window",
  "windows": ["main"],
  "platforms": ["windows", "macOS", "linux"],
  "permissions": ["updater:default"]
}
